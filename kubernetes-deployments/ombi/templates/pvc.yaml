---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $.Values.pvcName }}
spec:
  storageClassName: cstor-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
