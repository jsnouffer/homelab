apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-plex
spec:
  capacity:
    storage: 900Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs-plex
  mountOptions:
    - hard
    - nfsvers=4.1
  nfs:
    path: /mnt/plex
    server: nas.jsnouff.net