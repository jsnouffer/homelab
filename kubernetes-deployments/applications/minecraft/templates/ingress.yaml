{{- with .Values.ingress }}
# CURRENTLY DISABLED DUE TO UNRELIABLE CONNECTION ISSUES
---
apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: minecraft-tcp
spec:
  entryPoints:
    - minecraft
  routes:
  - match: HostSNI(`*`)
    services:
    - name: minecraft-mcserver
      port: 25565
{{- end }}