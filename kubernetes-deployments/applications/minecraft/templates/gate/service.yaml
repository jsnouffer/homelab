---
apiVersion: v1
kind: Service
metadata:
  name: gate
spec:
  selector:
    app.kubernetes.io/component: proxy
    app.kubernetes.io/name: gate
  ports:
  - name: minecraft
    port: 25565
    protocol: TCP
    targetPort: minecraft
  type: LoadBalancer