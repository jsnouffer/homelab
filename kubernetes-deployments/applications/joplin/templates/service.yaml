apiVersion: v1
kind: Service
metadata:
  name: joplin-server
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/name: joplin-server
  type: ClusterIP
