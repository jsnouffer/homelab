apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llama-nfs-plex-pool
  namespace: llama
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 250Gi
  storageClassName: llama-nfs-plex-pool
  volumeMode: Filesystem
  volumeName: llama-nfs-plex-pool

