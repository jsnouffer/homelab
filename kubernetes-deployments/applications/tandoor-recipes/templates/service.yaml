---
apiVersion: v1
kind: Service
metadata:
  name: tandoor-recipes
spec:
  selector:
    app: tandoor-recipes
  ports:
  - port: 80
    targetPort: http
    name: http
    protocol: TCP
  - port: 8080
    targetPort: gunicorn
    name: gunicorn
    protocol: TCP
  type: LoadBalancer