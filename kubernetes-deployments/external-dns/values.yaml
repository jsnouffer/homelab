external-dns:
  provider: cloudflare
  cloudflare:
    secretName: cloudflare-api-token
    proxied: false
  extraArgs:
    cloudflare-dns-records-per-page: "5000"
  resources:
    limits:
      cpu: 50m
      memory: 48Mi
    requests:
      cpu: 5m
      memory: 24Mi