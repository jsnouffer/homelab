external-dns:
  provider: cloudflare
  cloudflare:
    secretName: cloudflare-api-token
    proxied: false
  extraArgs:
    cloudflare-dns-records-per-page: "5000"
  resources:
    limits:
      cpu: 50m
      memory: 64Mi
    requests:
      cpu: 5m
      memory: 32Mi