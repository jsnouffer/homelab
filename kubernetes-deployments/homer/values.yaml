homer:
  ingress:
    main:
      enabled: true
      hosts:
        - host: homer.jsnouff.net
          paths:
            - path: /
              service:
                name: homer
                port: 8080

  persistence:
    config:
      enabled: true
      type: configMap
      name: homer-config
      mountPath: /www/assets
